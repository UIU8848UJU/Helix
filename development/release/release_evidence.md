# Release Evidence — REPO-003 (Task pool refactor: extract shared Task Runtime)

- schema_version: "2.1"
- work_item_id: REPO-003
- task_name: "Extract generic Task Runtime (packages/jobs) from apps/ssh-mcp, wire SSH Runtime, regression green"
- date: "2026-08-08"
- release_ref: development/releases/REPO-003-handoff.md

## Verification command and result

- command: `npm run check --workspace @helix/ssh-mcp && npm run test --workspace @helix/ssh-mcp && npm run build && npm run test`
- exit_code: 0
- environment: DEV-WINDOWS / host-shell / UNIT-LOCAL + BUILD-LOCAL
- result: **69 passed / 1 skipped** (23 @helix/jobs + 46/1 @helix/ssh-mcp); root build PASS

## Build

- `npm run build` (root, workspaces order `["packages/*", "apps/*"]`) → PASS
  - `packages/jobs` → build/ (gitignored, regenerated by `prepare` on install)
  - `apps/ssh-mcp` → build/
- typecheck: `npm run check --workspace @helix/ssh-mcp` → tsc --noEmit exit 0

## Test

| Suite | Passed | Skipped |
|-------|--------|---------|
| @helix/jobs | 23 | 0 |
| @helix/ssh-mcp | 46 | 1 |
| **Total** | **69** | **1** |

Skipped test is the Unix-only remote job local e2e on win32 (deferred to LINUX-SSH-TARGET).

## Independent confirmation

- G15 independent reviewer re-ran the exact verification command and the fresh-checkout
  simulation (rm -rf packages/jobs/build → npm install → check exit 0). PASS.
  See quality/independent_review.md and development/reviews/G15-delivery-quality.yaml.

## Validator

- `validate_tdd_workspace.py development --profile development-ready` → PASS
- `validate_tdd_workspace.py development --profile delivery-ready` → PASS
