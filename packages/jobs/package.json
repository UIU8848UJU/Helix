{
  "name": "@helix/jobs",
  "version": "0.1.0",
  "private": true,
  "description": "Generic Task Runtime model shared by ssh-mcp and browser-mcp: task state machine, TaskPool (bounded queue, worker limit, priority, timeout, retention, metrics), wire-protocol helpers, Semaphore and shell quoting.",
  "type": "module",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "files": ["build"],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "prepare": "npm run build"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
