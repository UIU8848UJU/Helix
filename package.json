{
  "name": "helix-ai-infra",
  "version": "0.1.0",
  "private": true,
  "description": "AI infrastructure monorepo for secure agent tools and MCP servers.",
  "type": "module",
  "workspaces": [
    "apps/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "check": "npm run check --workspaces --if-present"
  }
}
